compound_components:
  version: 0.3
  classes:
  - name: alu_unit
    primitive_type: alu_unit
    attributes:
      technology: must_specify
      clockrate: must_specify
      datawidth: must_specify

    subcomponents:
      # bare bones initial design
      - name: intadder
        class: intadder
        attributes:
          technology: technology
          datawidth: datawidth

    actions:
      - name: int_instruction
        subcomponents:
          - name: intadder
            actions:
              - name: add
